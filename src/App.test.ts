import { describe, expect, it } from 'vitest';

// TODO: remove temporary stub so `vite --test` doesn't error
describe('Temporary: REMOVE ME', () => {
  it('temp test', () => {
    expect(true).toBeTruthy();
  });
});
